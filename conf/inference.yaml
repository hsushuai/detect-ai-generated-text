model:
    backbone_path: microsoft/deberta-v3-small
    max_length: 1024
    dropout_rate: 0.0
    gradient_checkpointing: true

predict_params:
    checkpoint_path: models/finetuned-deberta-v3-small-best.pth.tar
    per_device_eval_batch_size: 1